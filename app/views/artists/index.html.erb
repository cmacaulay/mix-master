<h1>All Artists</h1>
<div class="all-artists">
  <%= link_to("New artist", new_artist_path) %>
  <ul>
    <% @artists.each do |artist| %>
      <li><%= link_to(artist.name, artist_path(artist), {class: "artist", id: "artist_#{artist.id}"} )%></li>
    <% end %>
  </ul>
</div>
